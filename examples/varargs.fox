fn add(a: int, b: int) -> int {
	return a + b;
}

fn sum(vararg ns: []int) -> int {
	let mut total = 0;
	for n in ns {
		total += n;
	}
	return total;
}

let x = add(1, 4);
let y = sum(10, 20, 30);

@print(x);
@print(y);
